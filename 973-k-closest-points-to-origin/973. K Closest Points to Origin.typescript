function kClosest(points: number[][], k: number): number[][] {
    const heap: MaxHeap<number[]> = new MaxHeap(p=> p[0]*p[0] + p[1]*p[1]);
    for (const [x, y] of points) {
        heap.push([x, y])
        if (heap.size() > k ) heap.pop()
    }
    return heap.toArray()
};